/*
    Scrivere un programma che date due masse in Kg calcoli la
    forza di attrazione gravitazionale presente tra le due:
    F = G*(M1*M2)/2
    G = 6,67 * 10^(-11)

    Esempio di input:   152 654 
    Output Atteso:      0,000000331525.... N
*/

/*
    Scrivere un programma che dato il coefficiente di attrito viscoso di un fluido
    e la massa di un corpo calcoli (in modo molto semplificato) la velocità
    limite della caduta del corpo nel fluido (trascurando la forza di Archimede).

    V = mg/c            se 0 < c < 10000
    V = sqrt(mg/c)      se c >= 10000
    g = 9,8

    Esempio di input:   100 56
    Output Atteso:      5,488 m/s
*/

/*
    Scrivere un programma che presi in input una serie di n
    valori terminata da 0 ne calcoli la moltiplicazione.

    Esempio di input:   3 5 -6 0 
    Output Atteso:      -90
*/


/*
    Scrivere un programma che dato in input il numero di iterazioni,
    stampi a schermo la sequenza di fibonacci fino all'iterazione indicata:
    f(0) = 1
    f(1) = 1
    f(2) = f(0) + f(1) = 2
    f(3) = f(1) + f(2) = 5
    ...
    f(n) = f(n-1) + f(n-2)

    Esempio di input:   5
    Output Atteso:
        f(0) = 1
        f(1) = 1
        f(2) = 2
        f(3) = 5
        f(4) = 7
*/

/*
    Scrivere un programma che dati in input i cateti di un
    triangolo rettangolo, lo stampi a schermo tramite "##"

    Esempio di input:   5 8
    Output Atteso:
        ##
        ####
        ########
        ##########
        ##############
*/